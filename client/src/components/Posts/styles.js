import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  mainContainer: {
    display: "flex",
    alignItems: "center",
  },
  smMargin: {
    margin: 8,
  },
  actionDiv: {
    textAlign: "center",
  },
});
